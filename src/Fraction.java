import java.util.ArrayList;
import java.io.File;
public class Fraction {
    // data fields
    ArrayList<Integer> numerators = new ArrayList<Integer>();
    ArrayList<Integer> denominators = new ArrayList<Integer>();


    /**
     * Fraction()
     * ----------
     * Constructor #1:
     * PRE: denominator != 0
     */
    public Fraction() {
        
    }

    // first: read file

    // while reading file, reduce the fraction first, then store it in the arrayList

    // call the FractionCounter to count the fraction card with each element int the arrayList
}